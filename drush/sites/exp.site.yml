local:
    root: /var/www/exp/docroot
    uri: 'http://local.exp.com'
    host: local.exp.com
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
